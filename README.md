# 🛰️ Система Мониторинга Wi-Fi Покрытия Карьера (Mikrotik & RTK)

Система предназначена для непрерывного сбора, анализа и визуализации данных о качестве Wi-Fi покрытия (RSSI, TxRate/RxRate) и GPS-координатах буровых установок (CPE Mikrotik), а также для мониторинга статуса базовой станции RTK.

Архитектура проекта включает отдельный GUI для управления, фоновые сервисы для сбора данных и базу данных SQLite для хранения метрик.

---

## 🛠️ Архитектура Проекта

Проект состоит из трех ключевых компонентов, работающих параллельно:

1.  **GUI (app_gui.py):** Центральный интерфейс для оператора, позволяющий запускать/останавливать мониторинг для каждой установки и просматривать статус системы.
2.  **Коллекторы данных:** Фоновые процессы, отвечающие за сбор метрик.
3.  **Базы данных (SQLite):** Хранилище оперативных и исторических данных.

| Компонент | Назначение | Технологии |
| :--- | :--- | :--- |
| `data_collector.py` | Сбор метрик Wi-Fi и GPS с **Mikrotik CPE** (для каждой установки). | Python, Paramiko (SSH), SQLite |
| `rtk_collector_service.py` | Непрерывный мониторинг **Базовой Станции RTK** (статус). | Python, SQLite |
| `app_gui.py` | Управление, визуализация (Карта, Графики) и отображение статусов. | Python, Tkinter, Pandas, Pillow |

---

## 🚀 Установка и Запуск

### 1. Требования

Для работы проекта необходим Python 3.x.

### 2. Установка Зависимостей

Установите все необходимые библиотеки, используя файл `requirements.txt`:

```bash
pip install -r requirements.txt
